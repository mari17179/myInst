
<nav class="navbar navbar-expand-lg navbar-dark navbar-custom fixed-top">
  <div class="container">
    <a class="navbar-brand" style="font-size:3vw" href="#">Inst</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarResponsive">

      <% if current_user %>

      <ul class="navbar-nav ml-auto" style="font-size:1.5vw; color:white;">
        <li class="nav-item">
          <%= current_user.username %>
        </li>
        <li class="nav-item" style="margin-right:5px; margin-left:5px;">
            <%= image_tag(current_user.user_photo_thumbnail) %>
        </li>
        <li class="nav-item" style="margin-right:5px; margin-left:5px;">
           |
        </li>
        <li class="nav-item">
          <%= link_to "Edit Account", edit_user_registration_path, {:style=>'color:white;', :class => "css_class"} %>
        </li>
        <li class="nav-item" style="margin-right:5px; margin-left:5px;">
           |
        </li>
        <li class="nav-item">
          <%= link_to 'Sign out', destroy_user_session_path, method: :delete  %>
        </li>
      </ul>

      <% else %>

      <ul class="navbar-nav ml-auto" style="font-size:2vw;">
          <%= link_to 'Sign up', new_user_registration_path, {:style=>'color:white;', :class => "css_class"}  %>
        </li>
        <li class="nav-item" style="margin-right:5px; margin-left:5px;">
           |
        </li>
        <li class="nav-item">
          <%= link_to 'Log in', new_user_session_path, {:style=>'color:white;', :class => "css_class"}  %>
        </li>
      </ul>

      <% end %>

    </div>
  </div>
</nav>

<header class="masthead text-center text-white">
    <div class="masthead-content">
      <div class="container">
        <h1 class="masthead-heading mb-0">Wecome to Inst!</h1>
        <h2 class="masthead-subheading mb-0">Watch all new posts of your friends :)</h2>
        <h2 class="masthead-subheading mb-0">
          <% if current_user %>
           <a>Watch!</a>
          <% else %>
          <%= link_to 'Log in and watch!', new_user_session_path, {:style=>'color:white;', :class => "css_class"}  %> </h2>
          <% end %>
      </div>
    </div>
    <div class="bg-circle-1 bg-circle"></div>
    <div class="bg-circle-2 bg-circle"></div>
    <div class="bg-circle-3 bg-circle"></div>
    <div class="bg-circle-4 bg-circle"></div>
  </header>
